import { createTheme } from "@mui/material";
import "./theme.css";

export const theme = createTheme({
  palette: {
    primary: {
      main: "##ff8400",
    },
    success: {
      main: "#95c289",
    },
    secondary: {
      main: "#efc918",
    },
  },
});
